image: maven:latest

# Define the stages that the jobs will run in
stages:
  - test
  - deploy

# Maven test job
test:
  stage: test
  script:
    - mvn test

# GitLab Pages deployment job
pages:
  stage: deploy
  script:
    - mkdir public
    - cp -r apidocs/* public/
  artifacts:
    paths:
      - public
  only:
    - master
